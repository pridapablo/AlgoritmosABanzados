// Recursive implementation of the change problem.
#ifndef RECURSIVE_CHANGE_H_
#define RECURSIVE_CHANGE_H_

#include <vector>
#include <map>

using namespace std;

/*

*/
vector<int> recursiveChange(int change, vector<int> coinTypes)
{
    // Arreglo de pares: { numero de monedas, quién lo etiquetó (etiquetar de donde viene la solución) }
}

#endif // RECURSIVE_CHANGE_H_
